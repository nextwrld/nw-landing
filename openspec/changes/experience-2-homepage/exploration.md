## Exploration: experience-2-homepage

### Current State
The homepage is a server-rendered, locale-prefixed composition in `src/app/[locale]/page.tsx`, but it remains the Foundation-era Tailgrids landing: Hero, Features, About, Pricing, FAQ, three generic case cards, and Contact. Static page copy is loaded from typed ES/EN dictionaries; case content is local Markdown; metadata, canonical URLs, reciprocal hreflang, OG/Twitter, Organization JSON-LD, GTM bootstrap, and a small event helper already exist.

The authoritative specification requires a substantially different narrative and component model: problem recognition through operational diagnosis, an editorial light/dark chapter rhythm, real-product evidence, and all critical commercial content in initial HTML. Existing client boundaries are concentrated in the header/menu/theme/language controls, FAQ disclosure, contact form, and diagnosis hero. That is compatible with Foundation ADRs, but the new storytelling motion must remain optional and reduced-motion safe.

### Affected Areas
- `src/app/[locale]/page.tsx` — replace the Foundation section composition with the specified homepage narrative while retaining server-first assembly.
- `src/i18n/es.ts`, `src/i18n/en.ts`, `src/i18n/dictionaries.ts` — replace/extend the current homepage dictionary with an identical typed ES/EN Experience 2.0 shape.
- `src/components/Header/index.tsx`, `src/components/Header/menuData.tsx`, `src/components/Footer/index.tsx` — align navigation, mobile menu, diagnosis CTA, and footer information architecture with the specification.
- `src/components/Hero/index.tsx`, `src/components/Features/`, `src/components/About/`, `src/components/Pricing/`, `src/components/Faq/` — retire or reshape Foundation presentation components; their current copy and visual grammar do not meet the target.
- `src/components/Diagnostico/`, `src/components/Contact/`, `src/constants/links.ts` — evolve the direct external-calendar/generic-contact flow into the specified context-first diagnosis flow; preserve the hardened `/api/contact` boundary unless an approved integration requires a new endpoint.
- `src/components/SuccessCases/`, `markdown/success-cases/**`, `public/images/**` — source AION/JFHP/InmoCRM/automation evidence and imagery; current case slugs/content/assets are Gym Access OS, InmoCRM, and AI Sales Engine and do not provide the required AION showcase.
- `src/components/GoogleTagManager.tsx`, `src/utils/analytics.ts`, `src/components/Common/TrackedLink.tsx` — complete required event names and attach location-aware CTA, WhatsApp, service, case, and insight tracking.
- `src/app/[locale]/page.tsx`, `src/app/site.ts`, `src/utils/seo.ts` — update homepage title/description and add homepage-specific pertinent structured data without regressing existing canonical/hreflang/OG/Twitter behavior.
- `src/styles/index.css` — establish Experience 2.0 tokens, chapter backgrounds, responsive typography/layout, and reduced-motion rules; the current theme is broad Tailgrids compatibility CSS rather than a homepage design system.
- `tests/seo.test.ts`, `tests/localization.test.ts`, `tests/e2e/locale-navigation.spec.ts`, new focused tests — protect dictionary parity, server HTML/heading structure, metadata/schema, event contracts, responsive interaction, and reduced-motion behavior under strict TDD.

### Approaches
1. **Dedicated Experience 2.0 homepage composition** — Build section-specific server components from a typed homepage-content model, retaining only small interactive islands (header, FAQ, motion controller, form).
   - Pros: Matches the specified non-generic visual/narrative structure; preserves static SEO HTML; isolates each section for review and testing.
   - Cons: Replaces much of the Foundation landing presentation and needs a deliberate compatibility/retirement plan for old components.
   - Effort: High.

2. **Incrementally repurpose existing Tailgrids sections** — Rename and restyle Hero/Features/About/Pricing/FAQ/cases until they contain the new copy.
   - Pros: Smaller initial diff and less file creation.
   - Cons: The current abstractions enforce generic cards/grids, obscure the narrative, complicate AION and process visuals, and increase coupling to obsolete terminology.
   - Effort: Medium initially, High overall.

### Recommendation
Use a dedicated Experience 2.0 composition (Approach 1). Keep `page.tsx` as the server orchestration point, place all approved localized content in one typed homepage dictionary domain, and create intentionally section-specific presentational components instead of a universal card system. Preserve Foundation invariants: `/es` and `/en` remain canonical static routes; all commercial copy is server-rendered; client JavaScript is limited to interaction/motion; existing SEO helpers and contact validation are reused where applicable.

Implement as auto-chained, independently reviewable work units below the 800-line session budget where practical:

1. **Content contract, global shell, and SEO/tracking contract** — typed ES/EN homepage content, header/footer/menu/CTA vocabulary, metadata/schema, event vocabulary, and tests. Target <800 authored lines; establishes links and contracts before visual sections.
2. **Narrative foundation** — hero/system visual (static-first), problem recognition, operational cost/better-way, shared tokens, responsive and reduced-motion baseline. Target <800 lines; no unavailable case assets.
3. **Solution and method** — capabilities, AI treatment, Discover→Shape→Build→Launch→Evolve timeline, and focused motion island/tests. Target <800 lines.
4. **Evidence and differentiation** — AION showcase plus supporting cases, Why Next Wrld, optional compact Own Products, contingent on approved real assets and destination URLs. Target <800 lines; keep case-content migration separate if it becomes substantial.
5. **Diagnosis conversion and completion** — context-first diagnosis form/calendar handoff, WhatsApp CTA, FAQ, final CTA, conversion/e2e/accessibility verification. Target <800 lines; publication depends on calendar/form ownership decisions.

### Risks
- **Publication blocker:** the specified FAQ ownership answer cannot be published as an absolute claim until the client-ownership contractual policy is confirmed; the source explicitly requires legal formalization.
- **Publication blocker:** the required diagnosis sequence is context fields (Name, Company, Email, operational area) before time selection, but the current primary route opens a direct external Google Calendar URL. The calendar provider/embed/redirect mechanism and data handoff/privacy treatment are not evidenced in the repository.
- **Unavailable evidence/assets:** no AION product screenshots/mockups or AION destination URL are present; no JFHP source/content or route is present; existing images are generic/template/blog assets. Real approved AION/JFHP/automation assets and destination URLs are required before the evidence slice can be accepted.
- **Information architecture dependency:** the specified Services, How we work, Cases, Insights, and About links do not currently have dedicated routes; current navigation only targets homepage anchors. Confirm whether the first release uses anchors, approved destination routes, or intentionally unavailable links before publication.
- **Content evidence risk:** current InmoCRM Markdown describes a scalable platform and existing Gym Access OS/AI Sales Engine labels do not map cleanly to the specified AION/JFHP/automation evidence. Case copy must be editorially validated to avoid implying production results or invented metrics.
- **Tracking gap:** current analytics supports diagnosis, calendar, contact, case, and language events only; it lacks the specified WhatsApp, service, and insight names, and current homepage CTAs are not consistently tracked with locations.
- **Visual/motion risk:** current Tailgrids styling and large legacy SVG-heavy sections are not a reusable design-token system. New motion must honor `prefers-reduced-motion`, never hide HTML content, and avoid turning sections into client-rendered content.

### Ready for Proposal
Yes — product decisions are already authoritative. The proposal should treat the five items above as implementation dependencies/publication blockers, not reopen copy, layout, conversion, SEO, or motion choices. It should explicitly gate the evidence slice on approved real assets/URLs and the diagnosis/ownership answers, while allowing the server-first shell and narrative sections to proceed independently.

---

## Exploration: Homepage V3 (spec 1.0)

> Superseding addendum. The V2 exploration/proposal/design/tasks above describe the architecture that was implemented through slice `experience-2-homepage-08-admission`. Homepage V3 (spec 1.0, delivered by the user) is now the authoritative specification and inverts the implemented architecture: the homepage stops being a mega-landing with in-page anchor navigation and becomes the ecosystem entry door with ONE primary conversion ("Analizar mi operación"), EXACTLY 6 content sections, and section sub-pages as the primary navigation. This section maps the inversion against the real code.

### Current State (post-V2 implementation, branch `experience-2-homepage-08-admission`)

- **Composition** `src/app/[locale]/page.tsx`: admission-gated by `admitPublication(getPublicationConfig())`. Draft → Foundation (Tailgrids: Hero/Features/About/Pricing/Faq/SuccessCasesSection/Contact). Release/preview → `<ExperienceHome content={getHomepageContent(l)} />` + `homepageSchema` JSON-LD. The Experience branch composes 11 blocks: Hero, Problem, Impact, BetterWay, `#servicios` wrapper + Capabilities, ChapterDivider, `#metodo` wrapper + Method, ChapterDivider, `#nosotros` wrapper + Differentiation, ChapterDivider, `#casos` wrapper + EvidenceSection, ChapterDivider, Diagnosis, ChapterDivider, Faq, FinalCTA (`src/components/HomeExperience/ExperienceHome.tsx`).
- **Anchor machinery (the inverted architecture):** `ExperienceHome.tsx` `navAnchorId()` resolves approved `content.nav.items[i].destination` (`/#servicios`, `/#metodo`, `/#casos`, `/#nosotros` in ES; `/#services`, `/#method`, `/#cases`, `/#about` in EN) into `<div id={...}>` wrappers. `ExperienceHeader.tsx` (client) carries `SCROLLSPY_BY_SECTION` (capabilities→services, method→method, evidence→cases, differentiation→about), an IntersectionObserver that sets `activeNavPath`, and `handleAnchorClick()` smooth-scroll interception for `/#` links in both desktop nav and mobile menu. Foundation `Header/index.tsx` has the same interception in its `isHomeLike` branch; `Header/menuData.tsx` `buildApprovedNav()` flattens approved nav to localized paths. `localizedHref(locale, "/#x")` → `/{locale}/#x` (`src/utils/i18n-url.ts`). Header CTA: `localizedHref(l, "/#diagnosis")` (layout.tsx).
- **Routes:** only `src/app/[locale]/{page,pricing,diagnostico,contact,success-cases/[slug],privacy-policy,terms-of-service,legal-notice}`. There is NO `(site)` group, and NONE of the V3 section routes exist (`/es/servicios/*`, `/es/como-trabajamos`, `/es/casos`, `/es/insights`, `/es/nosotros`). Detail pages are Foundation-era `success-cases/[slug]` (slugs: gym-access-os, chatbot, crm); `/casos` listing does not exist. `src/app/sitemap.ts` publishes static paths + success-cases only. Locale convention: all routes under `[locale]/` with `generateStaticParams` (layout.tsx), `locales = ["es","en"]`, default `es` (`src/i18n/config.ts`); per-page `buildPageMetadata`/`localeAlternates` canonical+hreflang in `src/utils/seo.ts`.
- **Content contract** `src/content/homepage/{types,es,en,index,publication,manifest,evidence}.ts`: `HomepageContent` = locale, seo, nav.items (5: services, method, cases, about approved with `/#` destinations; insights approved:false, destination:null), hero, problem, impact, betterWay, capabilities, method, differentiation, evidence, faq, diagnosis, finalCta. `validateContentParity()` requires keys: problem, impact, betterWay, capabilities, method, differentiation, evidence, diagnosis, finalCta. `navAnchorPath()` resolves approved nav→anchor. Evidence: AION showcase approved:false; JFHP/automation/InmoCRM entries approved:false with null destinations; `verifiedCapabilities.aion = []` (empty allowlist); `public/images/experience/manifest.json` has 4 placeholder SVG entries; InmoCRM `markdown/success-cases/{es,en}/crm.md` already MVP-safe (FORBIDDEN_CLAIM_PATTERNS gate).
- **Publication gate (fail-closed):** `src/content/homepage/publication.ts` `getPublicationConfig()` reads `EXPERIENCE_PUBLICATION_STATUS` (draft default; `EXPERIENCE_PREVIEW=true` → preview). Draft validates parity only; release requires all 9 approval keys + evidence/manifest/AION/metadata validations; rejection throws `PublicationBlockedError` before output. `scripts/validate-experience-build.ts` calls it and exits 1; `package.json` build = `pnpm validate:experience-build && next build`; `vercel.json` buildCommand `pnpm build`.
- **Tests that encode the anchor architecture (must be retired/reworked for V3):** `tests/section-pages.test.ts` (SECTION-PAGE-001/002: exact `/#` destinations per locale, exactly 4 approved items, insights withheld, no `sectionPages` domain, release fail-closed); `tests/section-routes.test.ts` (SECTION-PAGE-003/004: asserts `[...section]/page.tsx` and `SectionPage.tsx` do NOT exist, every approved destination `^/#`, hero `secondaryCtaHref === "#evidence"`, `ExperienceHome` contains `servicesAnchor/methodAnchor/casesAnchor/aboutAnchor` + `<div id={servicesAnchor}>`, sitemap excludes `/es/servicios`, `/es/metodo`, `/es/casos`, `/es/nosotros`, `/en/services`, `/en/method`, `/en/cases`, `/en/about`); `tests/e2e/homepage-experience-preview.spec.ts` (nav hrefs equal `["/es#servicios","/es#metodo","/es#casos","/es#nosotros"]`, `#evidence`/`#diagnosis` ids, section sub-pages 404, scrollspy active class, 5 `.chapter-divider`, numbered eyebrows `05 · QUÉ PODEMOS CONSTRUIR` / `10 · PREGUNTAS FRECUENTES`); `tests/e2e/homepage-release.spec.ts` (draft retains Foundation; "no speculative links" regex rejects `/#(diagnosis|method|cases|insights|betterWay|finalCta)`). `tests/chapter-differentiation.test.ts` (CHAPTER-001) and `tests/experience-design.test.ts` (chapter tokens) will need renaming/rework to V3's 6-section chapter rhythm. Draft-isolation/a11y specs (homepage-shell/content/method/evidence/diagnosis/observability, shell-a11y, locale-navigation) are largely V3-compatible but pin V2 heading copy.
- **Analytics:** `src/utils/analytics.ts` EVENT_NAMES = diagnosis_cta_click, whatsapp_click, calendar_click, service_view, case_view, insight_view, contact_form_start/submit/success/error, language_change; `DIAGNOSIS_CTA_LOCATIONS = [header, hero, diagnosis_section, final]`; `TrackedLink` gates external activation events. V3 requires diagnosis_cta_click, **service_click, method_click, case_click**, whatsapp_click, language_change with `cta_location` context — i.e. rename service_view→service_click, case_view→case_click, add method_click. Note `commercial-observability/spec.md` in this change still names service_view/case_view/insight_view (docs `landingConstruction.md` #41 agree); the V3 user spec is authoritative and the OpenSpec specs will need a V3 revision.
- **Docs cross-reference (`docs/nextwrld2.0/`, untracked, V2-era but IA-consistent with V3):** `landingConstruction.md` §24-31 already prescribes the section-page IA now mandated by V3 (nav: Servicios / Cómo trabajamos / Casos / Insights / Nosotros + Diagnóstico CTA; `/servicios/software-a-medida`, `/servicios/sistemas-de-gestion`, `/servicios/automatizacion`; roles of `/nosotros`, `/casos`, `/insights`). `seo.md` §10-13, 31-34 (small main nav, URL strategy prefers `/es/servicios/software-a-medida`, URL-migration rule). `estructuraDeLaWeb.md` (14-section homepage V2) §21-22 contains the AION Wellness featured-case copy ("AION es un producto formalmente propiedad de Next Wrld"), JFHP, InmoCRM-as-MVP, and §29/§30 conversion hierarchy + H1/H2. `next-wrld-landing-design-copy-specification (1).md` §13-14 has the AION-dominant evidence copy (no invented metrics; real product UI required) and §26 CTA vocabulary. The V3 user spec (45 sections) is the source of truth; these docs supply copy/detail for the section pages.

### Affected Areas (V3)

- `src/app/[locale]/page.tsx`, `src/components/HomeExperience/ExperienceHome.tsx` — 6-section composition (01 Hero, 02 Problema→Transformación, 03 Qué resolvemos, 04 Cómo trabajamos, 05 Trabajo real AION-dominant, 06 Diagnóstico offer-only), no anchor wrappers, no chapter dividers as nav devices, no FAQ/FinalCTA on homepage.
- NEW routes: `src/app/[locale]/servicios/[slug]/page.tsx` (3 services), `src/app/[locale]/como-trabajamos/page.tsx`, `src/app/[locale]/casos/page.tsx`, `src/app/[locale]/insights/page.tsx`, `src/app/[locale]/nosotros/page.tsx`; `diagnostico` rebuilt as the context-first offer+form page (V2 homepage Diagnosis form + `homepage_diagnosis` source + calendar/WhatsApp handoff move there). Route conventions: locale-prefixed server components, per-route metadata/canonical/hreflang via existing `buildPageMetadata`, sitemap extension, parity for ES/EN slugs.
- RETIRE: anchor machinery (ExperienceHeader scrollspy/`handleAnchorClick`, `navAnchorId`/`navAnchorPath`, `/#`-aware `localizedHref` branch can stay but no longer used for primary nav), ChapterDivider usage, homepage Faq + FinalCTA + Differentiation + standalone Impact/BetterWay, `/#diagnosis` CTA destinations (→ `/es/diagnostico`).
- Content contract: `HomepageContent` reshaped to 6 sections + nav with route destinations; remove impact/betterWay/differentiation/faq/finalCta keys (content redistributes: Why Next Wrld → `/nosotros`, framework → `/como-trabajamos`, FAQ → section pages/`/diagnostico`); nav items become `services` (submenu of 3), `como-trabajamos`, `casos`, `insights`, `nosotros` with `destination: "/es/..."` routes. `validateContentParity` updated (6-section invariant, no `/#` destinations, no empty destinations).
- Publication gate: extend approvals (route/content approvals; "homepage must never link to empty content" becomes a release validator — every nav/footer/CTA destination must resolve to a real, content-complete route); evidence validators reused as-is for AION/JFHP/InmoCRM (still placeholder-blocked; AION needs real UI + destinations).
- Tests: rewrite `section-pages.test.ts`/`section-routes.test.ts` into section-route tests (routes exist, render, ES/EN parity, sitemap includes them); rewrite `homepage-experience-preview.spec.ts` (nav hrefs = section routes, 6 sections, no 404s); adjust `homepage-release.spec.ts` "no speculative links" to route-based no-empty-content checks; update `homepage-contracts/admission/observability/chapter-differentiation/experience-design` for the new shape and event vocabulary; add route-level a11y/SEO e2e.
- Analytics/SEO: `analytics.ts` event rename + `method_click`; `seo.ts` per-route metadata helpers (already generic); `sitemap.ts` + section routes; per-section schema; `homepageSchema` FAQ moves off homepage.
- `src/i18n/{es,en}.ts` — `experienceFooter` columns align with V3 footer (Servicios/Empresa/Contacto + section links); `menu` vocabulary for 5 section labels + submenu.
- `Header/menuData.tsx` — `buildApprovedNav` must emit V3 route items (Servicios with submenu — `Menu` type already supports submenu); Foundation `buildMenuData` stays only while draft retains Foundation.

### Approaches

1. **A — Section sub-pages + refactored homepage reusing V2 components.** Build the 8 section routes as thin page shells and recompose the homepage to 6 sections, reusing Capabilities/EvidenceSection/Diagnosis/Method components and the existing content dictionary where the copy overlaps. Pros: least new visual code; reuses hardened diagnosis/evidence flows. Cons: keeps the one-page content model (keys and copy shaped for a mega-landing), so section pages stay thin and risk violating "never link to empty content"; anchor machinery only partially removable; copy still largely lives in the homepage dictionary.
2. **B — Thin wrapper routes over existing one-page sections.** `/es/servicios/sistemas-de-gestion` renders the homepage Capabilities block, `/es/casos` renders EvidenceSection, etc. Pros: smallest diff, quick Fase 1. Cons: does not satisfy V3 (section pages must be real content destinations, not mirrored homepage sections); keeps anchor content model; SEO cannibalization; highest rework later. Not recommended.
3. **C — Full V3 composition.** New section components (Problema→Transformación, Qué resolvemos, Cómo trabajamos, Trabajo real), 7-8 new routes with per-route content, slim homepage content model (6 sections + nav routes), full retirement of anchor/scrollspy/chapter machinery and its tests. Pros: matches spec 1:1, maps cleanly onto the 3 delivery phases (Fase 1 skeleton/routes → Fase 2 copy → Fase 3 final), cleanest test story. Cons: highest churn (2 unit test files + 1 e2e spec rewritten, others adjusted); must sequence to never ship a homepage that links to empty routes.

### Recommendation

Adopt **C (full V3 composition)** with A's pragmatism where reuse is genuinely safe: reuse `Diagnosis` form state machine (`utils/diagnosis.ts`, strict `ContactPayload` union, `/api/contact` boundary) moved to `/es/diagnostico`; reuse `EvidenceSection`/`AIONProductShowcase`/`CaseEvidence` visuals inside 05 Trabajo real; reuse publication-gate machinery extended with route/no-empty-content validators; reuse `buildPageMetadata`/`localeAlternates`/`trackEvent`/`TrackedLink`. Retire the anchor architecture wholesale (ExperienceHeader scrollspy + anchor interception, `navAnchorId`/`navAnchorPath`, `/#`-destination nav model, ChapterDivider-as-nav, homepage Faq/FinalCTA/Differentiation) and delete/replace `section-pages.test.ts`, `section-routes.test.ts`, and the anchor assertions in `homepage-experience-preview.spec.ts`.

Structure V3 inside the existing change as three delivery phases:
- **Fase 1 (esqueleto):** new section routes + nav/footer route wiring + 6-section homepage skeleton with approved minimal copy, so no link points at empty content; retirement of anchor tests; new route/render/parity RED tests; draft gate extended to validate route completeness (release stays blocked).
- **Fase 2 (copy):** final ES/EN copy per section and per route; dictionary reshaped to 6-section model; `src/i18n` footer/menu vocabulary; copy parity tests.
- **Fase 3 (final):** visual/motion/SEO/analytics/accessibility/performance pass; event rename (service_click/method_click/case_click), per-route metadata+sitemap+schema, e2e/observability updates, evidence assets admission (AION real UI still required), release flip.

### Risks

- **Inversion churn:** 3 test files fully encode the anchor architecture and must be replaced, not patched; `experience-design`/`chapter-differentiation` reference the V2 chapter rhythm (5 dividers, numbered eyebrows) that V3 renumbers to 6 sections.
- **"Never link to empty content":** Fase 1 must deliver real content for every nav/CTA destination (including `/es/insights`, currently approved:false) or withhold the link; this becomes a fail-closed release validator.
- **Evidence still blocked:** AION showcase/JFHP/InmoCRM entries remain placeholder (approved:false, empty allowlist, placeholder manifest); 05 Trabajo real cannot release until real AION UI/destinations are approved. InmoCRM stays research/MVP per V3 — current MVP-safe copy already complies.
- **Event/spec conflict:** `commercial-observability/spec.md` and docs name service_view/case_view/insight_view; V3 names service_click/method_click/case_click. OpenSpec specs must be revised to V3 vocabulary before the observability slice lands.
- **Route naming/migration:** V3 mandates `/casos` while detail pages live at `/success-cases/[slug]`; per `seo.md` §34, URL migration (redirects or rename) needs an explicit decision. `/diagnostico` legacy message form coexists until the rebuilt page replaces it (V2 design already reserved `homepage_diagnosis` as a separate allowlist member — keep that separation).
- **Draft-vs-V3 homepage:** while the V3 composition is incomplete, the draft gate must keep serving a complete composition (currently Foundation with its own anchors); no partial 6-section homepage may go public.
- **Scope creep:** adding 8 routes + section content could exceed the 800-line review budget; re-slice Fase 1 into route-group units (servicios×3, institucional, casos/insights) with per-slice RED tests.

### Ready for Proposal

Yes — V3 is authoritative and already decision-grade. The next proposal should: (1) retire the one-page anchor architecture and its tests; (2) introduce section routes as the primary navigation with a no-empty-content release validator; (3) reshape `HomepageContent` to the 6-section model; (4) move the diagnosis form to `/es/diagnostico` (homepage 06 = offer only); (5) update the event vocabulary and OpenSpec specs to V3 names; (6) keep evidence admission and publication gates, still blocked on real AION/JFHP assets and destinations; (7) plan the three delivery phases (esqueleto → copy → final) as the apply structure.
